// math
def fib(x) = case( 
               x < 2, 
               1, 
               fib(x - 2) + fib(x - 1) 
            )


def fac(x) = case( 
               x == 1, 
               1, 
               x * fac(x - 1) 
            )

